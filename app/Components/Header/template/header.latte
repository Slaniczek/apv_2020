
<header class="header">
    <div class="header__container">
        <nav class="header__nav">
            <a href="{plink Homepage:}" class="header__link {if $presenter->isLinkCurrent('Homepage:')}header__link--active{/if}">Dashboard</a>
            <a href="{plink Users:default}" class="header__link {if $presenter->isLinkCurrent('Users:default')}header__link--active{/if}">Persons</a>
            <a href="{plink Locations:default}" class="header__link {if $presenter->isLinkCurrent('Locations:default')}header__link--active{/if}">Locations</a>
        </nav>
    </div>
</header>